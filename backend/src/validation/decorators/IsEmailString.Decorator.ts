import {registerDecorator, ValidationOptions, Validator} from "class-validator";

// own validation decorator to check if string is a valid email
export function IsEmailString(validationOptions?: ValidationOptions) {
  // TODO: to be implemented

}
